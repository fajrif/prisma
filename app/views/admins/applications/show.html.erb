<% title_page("Application Details") %>
<div class="row">
	<div class="col-12">
		<div class="card">
			<div class="card-body">
				<div class="form-actions text-end">
					<%= link_to 'Destroy', admins_application_path(@application.id), :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-danger' %>
					<%= link_to 'View All', admins_applications_path, :class => 'btn btn-light' %>
				</div>
				<div id="show-table">
          <%= render "details" %>
				</div>
			</div>
		</div>
	</div>
</div>
